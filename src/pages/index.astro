---
import { getEntry, render } from 'astro:content';
import Basic from '../layouts/Basic.astro';

const page = await getEntry('pages', 'index');
const { Content } = await render(page);
---

<Basic title={page.data.title}>
    <Content />
</Basic>
